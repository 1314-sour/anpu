# 设备接入功能变更记录

> 时间：2026-01-23

## 修改内容

1. `src/router/index.js`
   - 新增 `device-access/safety`、`device-access/monitor`、`device-access/firefight` 三个后台管理路由，分别指向安全生产、监控、消防设备接入页面，并配置 `meta.breadcrumb` 以驱动面包屑。

2. `src/views/Admin/index.vue`
   - 在原有“设备中心”一栏下方增加了新的一级菜单“设备接入”，内含三个下拉项并绑定到对应的路由。
   - 在 `breadcrumbMap` 中补充了“设备接入”及三种子页面的路径映射，保持面包屑跳转一致。

3. 新建页面文件
   - `src/views/Admin/ProdEqui.vue`
   - `src/views/Admin/MonitorEqui.vue`
   - `src/views/Admin/FirefightEqui.vue`

   以上三个文件暂以前端占位内容呈现，后续可填充实际的设备接入业务。

> 注：本次调整仅涉及 `src/router` 与 `src/views/Admin` 下的代码，未更改现有项目结构。

## 后续更新

- 2026-01-23：`src/views/Admin/ProdEqui.vue`、`MonitorEqui.vue`、`FirefightEqui.vue` 的前端内容改为与 `DataSummary.vue` 相同的占位样式（`el-empty`），与现有页面风格保持一致。
- 2026-01-23：新增“其他设备接入”子菜单
  - `src/router/index.js`：增加 `device-access/other` 路由指向 `OtherEqui.vue`，并设置 `breadcrumb = ['设备接入', '其他设备接入']`。
  - `src/views/Admin/index.vue`：在“设备接入”一级菜单中追加“其他设备接入”项，并在 `breadcrumbMap` 中映射 `'/admin/device-access/other'`。
  - `src/views/Admin/OtherEqui.vue`：新建页面文件，使用 `el-empty` 占位提示“其他设备接入 - 开发中”。
- 2026-01-23：在“数据汇总”板块后新增三个一级菜单（无二级菜单）
  - `src/router/index.js`：新增 `api-interface`、`data-mining`、`alarm-push` 路由，分别关联到对应页面并设置 `breadcrumb = ['数据汇总', <页面名>]`。
  - `src/views/Admin/index.vue`：在“数据汇总”菜单后依次加入“API接口”“数据挖掘与应用”“报警信息推送”三个一级菜单项，并在 `breadcrumbMap` 中配置各自的路由映射。
  - 新建页面文件：`src/views/Admin/ApiInterface.vue`、`src/views/Admin/DataMining.vue`、`src/views/Admin/AlarmPush.vue`，均使用 `el-empty` 占位提示当前开发状态。
- 2026-01-23：将 “API接口 / 数据挖掘与应用 / 报警信息推送” 调整为独立一级菜单
  - `src/router/index.js`：更新上述三条路由的 `meta.breadcrumb`，仅保留各自的标题（去除“数据汇总”前缀），确保它们以一级菜单的身份出现在面包屑中。


