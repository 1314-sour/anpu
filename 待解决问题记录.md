1.监控视频切片保存时长和本地保留的时长，以供降低内存占用。2.公网访问目前已经实现，即可以异地登录，但是仅供测试版本，后面需要修改域名和搭建服务器3.目前打算将设备列表中所有的设备都映射到设备状态中，需要考虑原始API数据中的字段和当前预设的字段能否契合。但是通过API导入数据没有这个分类设备类型的字段，和当前系统对接标注设备类型的时候需要分类。4.API接口集成，不要细分多类API接口，保证每一个设备信息捆绑集成一个API接口。***5.网页最小化的时候如果有报警在桌面最右下角要有弹窗（参考文档）***6.不能完全copy原系统，像前纺后纺的这种分组毫无意义，7.账号管理分级的功能待实现，***关于测试部分的问题******8.设备组态编辑和重构，需要哪些组件和组件对应的响应条件以及可编辑权限***9.设备相关的***变量字段管理***的tab页需要可导入表格形式（优先），也需要手动添加相关配置变量10.网关设备(已接通数据流)，设备导入后面的tab功能页11\.接入真实地图：去高德或百度地图开放平台申请一个 Key，使用 vue-amap 或直接引入 JS API，替换掉代码中的灰色 ECharts 背景12\.大屏适配 (Scale)：可视化大屏通常需要在大分辨率屏幕（如 4K 或拼接屏）上展示。你可以使用 CSS transform: scale() 方案，根据屏幕宽高比动态缩放整个容器，确保在任何屏幕上布局不乱13.WebSocket 接入：右侧的报警通常是实时的。建议使用 WebSocket 替换掉代码中的 setInterval，实现服务器一旦有报警，前端立刻弹出